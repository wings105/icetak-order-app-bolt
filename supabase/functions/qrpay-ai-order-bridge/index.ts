// @ts-nocheck
import 'jsr:@supabase/functions-js/edge-runtime.d.ts';import{createClient}from'https://esm.sh/@supabase/supabase-js@2';const db=createClient(Deno.env.get('SUPABASE_URL')||'',Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'',{auth:{persistSession:false}}),H={'content-type':'application/json','access-control-allow-origin':'*'};const out=(x,s=200)=>new Response(JSON.stringify(x),{status:s,headers:H}),t=v=>v==null?'':String(v).trim(),digits=v=>{let d=t(v).replace(/\D/g,'');if(!d)return'';if(d[0]=='0')d='60'+d.slice(1);else if(d[0]=='1')d='60'+d;else if(!d.startsWith('60'))d='60'+d;return d};async function auth(r){const k=r.headers.get('x-qrpay-ai-token')||'',q=await db.from('private_runtime_settings').select('setting_value').eq('setting_key','qrpay_ai_worker_token').maybeSingle();return!!k&&!q.error&&k===q.data?.setting_value}const allowed=new Set(['status','matched_conversation_id','matched_phone','matched_customer_name','match_score','match_reason','extraction','evidence','locked_at','completed_at','updated_at','next_attempt_at','last_error']);function title(k,x){const s=(t(k)+' '+t(x)).toLowerCase();if(/acrylic|akrilik/.test(s))return'Acrylic Cake Topper';if(/edible/.test(s))return'Edible Image';if(/wafer/.test(s))return'Wafer Paper';if(/burn/.test(s))return'Burn Away Combo';return'Cake Topper'}
function inferDate(p){if(p.date_need)return p.date_need;const a=p?.evidence?.messages||[],s=a.filter(x=>x.direction==='inbound').map(x=>t(x.text)).join('\n').toLowerCase(),now=new Date();let m=[...s.matchAll(/\b(\d{1,2})\s*(?:hb|haribulan)\b/g)].at(-1);if(m){let d=Number(m[1]),mo=now.getUTCMonth()+1,y=now.getUTCFullYear();if(d<now.getUTCDate()-3)mo++;if(mo>12){mo=1;y++}return`${y}-${String(mo).padStart(2,'0')}-${String(d).padStart(2,'0')}`}const months={jan:1,januari:1,feb:2,februari:2,mac:3,mar:3,april:4,apr:4,mei:5,may:5,jun:6,julai:7,july:7,ogos:8,aug:8,august:8,sep:9,september:9,okt:10,oct:10,nov:11,dis:12,dec:12};m=[...s.matchAll(/\b(\d{1,2})\s+(jan|januari|feb|februari|mac|mar|april|apr|mei|may|jun|julai|july|ogos|aug|august|sep|september|okt|oct|nov|dis|dec)\b/g)].at(-1);if(m)return`${now.getUTCFullYear()}-${String(months[m[2]]).padStart(2,'0')}-${String(Number(m[1])).padStart(2,'0')}`;return null}
function canon(p){p=structuredClone(p||{});p.customer=p.customer||{};if(p.customer.phone)p.customer.phone=digits(p.customer.phone);p.date_need=inferDate(p);if(Array.isArray(p.items))p.items=p.items.map(x=>({...x,title:title(x?.k,x?.title)}));const items=p.items||[],critical=[];if(!p.customer.phone)critical.push('customer');if(!items.length)critical.push('product');if(items.some(x=>!x.qty))critical.push('qty');if(items.some(x=>!t(x.size)&&['edible','acrylic'].includes(t(x.k))))critical.push('variation');const detail=Math.max(.45,Math.min(.97,1-critical.length*.18-(p.date_need?0:.05)));p.ai_missing=[...new Set([...(p.date_need?[]:['date']),...critical])];p.ai_confidence={customer:Number(p.match_score||0),payment:.98,order_detail:Number(detail.toFixed(2))};if(items.length)for(const x of items)x.customization={...(x.customization||{}),ai_confidence:p.ai_confidence,ai_missing:p.ai_missing};return p}function canDraft(p){return/^601\d{8,9}$/.test(digits(p?.customer?.phone))&&Array.isArray(p?.items)&&p.items.length>0}function release(p){const c=p.ai_confidence||{};return c.customer>=.90&&c.payment>=.90&&c.order_detail>=.90&&!p.ai_missing.some(x=>['customer','product','qty','variation'].includes(x))}
async function create(job,p,token){const{data,error}=await db.rpc('icetak_auto_create_qrpay_order',{p_job_id:job.id,p_payload:p,p_internal_token:token});if(error)throw error;const oid=t(data?.order_db_id);if(oid){const auto=release(p);await db.from('orders').update({production_approved:auto,admin_status:auto?'Ready to Process':'AI Draft — Check Needed',status:auto?'Ready to Process':'AI Draft — Check Needed',tab:'progress',admin_remark:(t(p.admin_remark)+'\nAI CONFIDENCE: customer '+Math.round(p.ai_confidence.customer*100)+'% | order '+Math.round(p.ai_confidence.order_detail*100)+'% | payment 98%\nMissing: '+(p.ai_missing.join(', ')||'None')).slice(0,3000),updated_at:new Date().toISOString()}).eq('id',oid);if(auto)await db.rpc('enqueue_clickup_production_order',{p_order_id:oid});await db.from('admin_order_notification_queue').update({event_type:auto?'ai_order_created':'ai_order_draft_created',status:'pending',scheduled_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('order_id',oid);await db.rpc('icetak_kick_admin_order_notification',{p_order_id:oid})}return data}
Deno.serve(async r=>{if(r.method==='OPTIONS')return new Response('ok',{headers:H});if(r.method!=='POST')return out({error:'POST required'},405);if(!await auth(r))return out({error:'Unauthorized'},401);const token=r.headers.get('x-qrpay-ai-token')||'',b=await r.json().catch(()=>({}));try{if(b.action==='claim'){const{data,error}=await db.rpc('claim_qrpay_ai_jobs',{p_limit:Math.max(1,Math.min(10,Number(b.batch_size||3)))});if(error)throw error;return out({ok:true,jobs:data||[]})}if(b.action==='update_job'){const patch={};for(const[k,v]of Object.entries(b.patch||{}))if(allowed.has(k))patch[k]=k==='extraction'?canon(v):v;patch.updated_at=new Date().toISOString();const{data,error}=await db.from('qrpay_ai_jobs').update(patch).eq('id',b.job_id).select('*').maybeSingle();if(error)throw error;return out({ok:true,job:data})}if(b.action==='create_order'){const{data:job,error}=await db.from('qrpay_ai_jobs').select('*').eq('id',b.job_id).single();if(error)throw error;const p=canon(b.payload);if(!canDraft(p)){await db.from('qrpay_ai_jobs').update({status:'needs_review',extraction:p,evidence:p.evidence||{},locked_at:null,updated_at:new Date().toISOString(),last_error:'No usable customer phone or item'}).eq('id',job.id);return out({ok:true,result:{success:true,draft_not_created:true}})}return out({ok:true,result:await create(job,p,token)})}return out({error:'Unsupported action'},400)}catch(e){console.error(e);return out({ok:false,error:e instanceof Error?e.message:String(e)},500)}});