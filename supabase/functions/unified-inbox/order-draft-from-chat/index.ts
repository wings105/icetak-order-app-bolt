// @ts-nocheck
import 'jsr:@supabase/functions-js/edge-runtime.d.ts';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const U=Deno.env.get('SUPABASE_URL')||'',K=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'',db=createClient(U,K,{auth:{persistSession:false}}),H={'content-type':'application/json','access-control-allow-origin':'*','access-control-allow-methods':'POST,OPTIONS','access-control-allow-headers':'authorization,content-type'},out=(x:any,s=200)=>new Response(JSON.stringify(x),{status:s,headers:H});
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:H});if(req.method!=='POST')return out({ok:false,error:'POST required'},405);try{const body=await req.json().catch(()=>({}));if(!body.conversation_id&&!body.phone)return out({ok:false,error:'conversation_id or phone required'},400);const q=await db.from('private_runtime_settings').select('setting_value').eq('setting_key','qrpay_ai_worker_token').maybeSingle(),token=String(q.data?.setting_value||'');if(!token)throw Error('internal draft token missing');const r=await fetch(`${U}/functions/v1/order-draft-trigger`,{method:'POST',headers:{'content-type':'application/json','x-order-draft-token':token},body:JSON.stringify({...body,source_type:'chat_trigger',payment_mode:body.payment_mode||'prepaid',request_key:body.request_key||`staff:${body.conversation_id||body.phone}:${body.trigger_message_id||body.cutoff_at||new Date().toISOString()}`})}),j=await r.json().catch(()=>({}));return out(j,r.status)}catch(e){return out({ok:false,error:e instanceof Error?e.message:String(e)},500)}});