name: Patch Shipping Manual AWB Link
on:
  push:
    branches: [main]
    paths:
      - 'scripts/patch_shipping_manual_awb_link.py'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.x'
      - run: python scripts/patch_shipping_manual_awb_link.py
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: icetak-admin/package-lock.json
      - name: Build Admin V2
        run: npm ci && npm run build
        working-directory: icetak-admin
      - name: Commit Shipping patch
        run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add icetak-admin/src/pages/Shipping.tsx
          git commit -m "add manual AWB order linking to Shipping V2"
          git push
